"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.doctorsAnalytics = void 0;
const appointment_model_1 = __importDefault(require("../models/appointment.model"));
const doctorPatient_model_1 = __importDefault(require("../models/doctorPatient.model"));
const report_model_1 = __importDefault(require("../models/report.model"));
// DOCTORS ANALYTICS
const doctorsAnalytics = async (doctorId) => {
    try {
        const today = new Date();
        const todayDateOnly = today.toISOString().split("T")[0];
        const startOfToday = new Date(todayDateOnly + "T00:00:00.000Z");
        const endOfToday = new Date(todayDateOnly + "T23:59:59.999Z");
        // Total patients connected to this doctor
        const totalPatients = await doctorPatient_model_1.default.countDocuments({ doctorId });
        // Pending appointments (all upcoming pending)
        const pendingAppointments = await appointment_model_1.default.countDocuments({
            doctorId,
            status: "pending",
        });
        // Reports generated by doctor
        const generatedReports = await report_model_1.default.countDocuments({ doctorId });
        // Today's appointments
        const todaysAppointments = await appointment_model_1.default.countDocuments({
            doctorId,
            date: { $gte: startOfToday, $lte: endOfToday },
        });
        return {
            success: true,
            analytics: {
                totalPatients,
                pendingAppointments,
                generatedReports,
                todaysAppointments,
            },
            date: {
                today: todayDateOnly,
                startOfToday: startOfToday.toISOString(),
                endOfToday: endOfToday.toISOString(),
            },
            message: "Analytics fetched successfully",
        };
    }
    catch (error) {
        return {
            success: false,
            message: error.message,
        };
    }
};
exports.doctorsAnalytics = doctorsAnalytics;
